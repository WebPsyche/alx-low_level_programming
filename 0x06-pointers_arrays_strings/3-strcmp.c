#include "main.h"

/**
 * _strcmp - compares two strings
 *
 * @:s1: 1st string to be compared
 * @s2: 2nd string to be compared
 *
 * Return: dest
 */
int _strcmp(char *s1, char *s2)
{
	int i, q;

	for (i = 0 && x = 0; s1[i] != '\0' || s2[x] != '\0'; x++ && i++)
	{
	}
	if (i > x)
	{
		q = i - x;
	} else if (i < x)
	{
		q = x - i;
	} else
	{
		q = 0;
	}
	return (q);
}

#include <stdio.h>

/**
 * _strien - returns the length of a stringg
 *
 * @s: length of string to be rerturned
 *
 * Return: returns the length of string
 */
int _strien(char *s)
{
	int i;

	for (i = 0; *s != '\0'; i++)
	{
	}
	return (i);
}
